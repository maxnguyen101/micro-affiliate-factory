---
const { title = 'Related guides', links = [] } = Astro.props;
---
<section class="card" style="margin-top:20px">
  <h3>{title}</h3>
  <ul>
    {links.map((l) => <li><a href={l.href}>{l.label}</a></li>)}
  </ul>
</section>
