#!/usr/bin/env bash
set -euo pipefail

echo "This project uses OpenClaw cron jobs instead of system cron by default."
echo "Create/update reminder jobs through the OpenClaw cron tool when needed."
echo "No cron jobs installed by this script."
